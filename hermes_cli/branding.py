"""User-facing product branding constants.

The Python package and many long-lived paths still use ``hermes`` internally
for compatibility. Keep visible command/product names here so the rebrand is
easy to audit without a risky module/package rename.
"""

PRODUCT_NAME = "Reuben Agent"
PRODUCT_NAME_UPPER = "REUBEN-AGENT"
CLI_COMMAND = "reuben"
LEGACY_CLI_COMMAND = "hermes"
